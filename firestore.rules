rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The application uses its least-privilege Cloud Run service identity.
    // Direct Firebase client access is denied by default until a reviewed,
    // tested product requirement introduces a narrower rule.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
