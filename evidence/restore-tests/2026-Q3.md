# Backup Restore Test — 2026 Q3

**Performed:** 2026-07-29 · **Operator:** dorkalev · **Criteria:** A1.2, A1.3, CC7.5, CC9.1

A real restore of a Firestore backup to an isolated database, verifying the backup is
recoverable (not just that backups exist). This is an actual drill, not a paper procedure.

## What was done
1. **Source backup:** `projects/shadow-dk-246464/locations/us-central1/backups/412f678f-…`
   — state READY, snapshot `2026-07-29T05:33:53Z` (from the daily schedule, 14-day retention).
2. **Restore:** `gcloud firestore databases restore --source-backup=… --destination-database=restore-drill-20260729`
   → restore operation ran to completion; a new FIRESTORE_NATIVE database
   `restore-drill-20260729` (us-central1) was created from the backup, created
   `2026-07-29T09:22:15Z`.
3. **Verify:** restored database confirmed present and healthy via
   `gcloud firestore databases describe`.
4. **Cleanup:** delete-protection disabled on the temp database and the database deleted;
   only `(default)` remains. No impact to production.

## Result
- **PASS** — a production backup was successfully restored to a working database within the
  same session. Recovery path is proven end-to-end.
- **RTO observed:** restore operation completed in well under the 8-hour RTO target
  (`policies/bcdr.md`). **RPO:** ≤24h (daily backups) + continuous PITR (1-hour window).
- Delete-protection behaved correctly (blocked deletion until explicitly disabled).

## Follow-up
None required. Next restore test: 2026-Q4 (quarterly per `policies/bcdr.md`). Sign-off = merge.
