{
  "name": "measurements",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "tsc --outDir .test-build --rootDir . --strict --module commonjs --target ES2022 --esModuleInterop --skipLibCheck src/*.ts test/*.ts && node --test .test-build/test/*.js"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.10.0",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.1"
  },
  "overrides": {
    "uuid": "11.1.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "typescript": "^5.6.3"
  }
}
