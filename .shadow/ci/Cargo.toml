[package]
name = "shadow-ci"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Deterministic SOC 2 compliance gates for GitHub PRs: per-PR audit + post-merge evidence archive with bypass detection"

[dependencies]
regex = "1"
serde_json = "1"

[profile.release]
strip = true
lto = true
