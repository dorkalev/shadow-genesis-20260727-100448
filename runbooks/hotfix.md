# Hotfix Runbook

Direct pushes to `main` are emergencies only: open an incident ticket (impact, root cause, why bypassed), then a backport PR to `staging` through the normal gates. The bypass detector records undocumented direct pushes.
