# Offboarding Runbook

On last day: revoke every grant in `policies/access-register.md`, capture before/after grant report, file under `evidence/lifecycle/`. Use `./atonement.sh --only ... ` / the offboard command for the before/after proof.
